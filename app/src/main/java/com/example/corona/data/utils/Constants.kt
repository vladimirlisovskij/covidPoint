package com.example.corona.data.utils

import com.example.corona.BuildConfig

object Constants {
    const val API_BASE_URL = BuildConfig.API_BASE_URL
    const val FLAG_BASE_URL = BuildConfig.FLAG_BASE_URL
    const val DB_VERSION = BuildConfig.DB_VERSION
    const val DB_NAME = BuildConfig.DB_NAME

    const val DISPATCHER_IO = "DISPATCHER_IO"
}